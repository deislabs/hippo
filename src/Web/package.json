{
    "dependencies": {
        "ansi-regex": ">=6.0.1",
        "bootstrap": "^5.1.3",
        "bulma": "^0.9.2",
        "font-awesome": "^4.7.0",
        "jquery": "^3.6.0",
        "jquery-validation": "^1.19.3",
        "jquery-validation-unobtrusive": "^3.2.12",
        "sass": "^1.48.0",
        "set-value": ">=4.1.0"
    },
  "devDependencies": {
        "del": "^6.0.0",
        "gulp": "^4.0.2",
        "gulp-dart-sass": "^1.0.2",
        "xterm": "^4.16.0"
  },
  "scripts": {
        "build": "npm install && gulp clean && gulp",
        "cibuild": "npm ci && gulp clean && gulp"
  }
}
